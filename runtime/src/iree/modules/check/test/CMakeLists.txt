################################################################################
# Autogenerated by build_tools/bazel_to_cmake/bazel_to_cmake.py from           #
# runtime/src/iree/modules/check/test/BUILD.bazel                              #
#                                                                              #
# Use iree_cmake_extra_content from iree/build_defs.oss.bzl to add arbitrary   #
# CMake-only content.                                                          #
#                                                                              #
# To disable autogeneration for this file entirely, delete this header.        #
################################################################################

iree_add_all_subdirs()

iree_lit_test_suite(
  NAME
    lit
  SRCS
    "failure.mlir"
    "success.mlir"
    "unavailable.mlir"
  TOOLS
    FileCheck
    iree-check-module
    iree-compile
    iree-run-module
  LABELS
    "hostonly"
)

# Each backend is defined explicitly via iree_check_single_backend_test_suite
# (instead of the iree_check_test_suite default-3-backend convenience macro)
# so that llvm-cpu can carry its own "nomips" label without also disabling
# the vmvx/vulkan-spirv variants: the compiler backend availability check is
# skipped when using a prebuilt host compiler (IREE_HOST_BIN_DIR), so
# IREE_TARGET_BACKEND_LLVM_CPU=OFF does not by itself stop llvm-cpu tests
# from being generated/run on a codegen-less target build (e.g. MIPS Phase 1,
# vmvx only).
iree_check_single_backend_test_suite(
  NAME
    check_vmvx_local-task
  SRCS
    "success.mlir"
  TARGET_BACKEND
    "vmvx"
  DRIVER
    "local-task"
  COMPILER_FLAGS
    "--iree-input-demote-f64-to-f32=false"
)

iree_check_single_backend_test_suite(
  NAME
    check_vulkan-spirv_vulkan
  SRCS
    "success.mlir"
  TARGET_BACKEND
    "vulkan-spirv"
  DRIVER
    "vulkan"
  COMPILER_FLAGS
    "--iree-input-demote-f64-to-f32=false"
)

iree_check_single_backend_test_suite(
  NAME
    check_llvm-cpu_local-task_generic
  SRCS
    "success.mlir"
  TARGET_BACKEND
    "llvm-cpu"
  DRIVER
    "local-task"
  COMPILER_FLAGS
    "--iree-input-demote-f64-to-f32=false"
    "--iree-llvmcpu-target-cpu=generic"
  LABELS
    "nomips"
)

iree_check_single_backend_test_suite(
  NAME
    check_failure_vmvx_local-task
  SRCS
    "failure.mlir"
  TARGET_BACKEND
    "vmvx"
  DRIVER
    "local-task"
  COMPILER_FLAGS
    "--iree-input-demote-f64-to-f32=false"
  RUNNER_ARGS
    "--expect_failure"
)

iree_check_single_backend_test_suite(
  NAME
    check_failure_vulkan-spirv_vulkan
  SRCS
    "failure.mlir"
  TARGET_BACKEND
    "vulkan-spirv"
  DRIVER
    "vulkan"
  COMPILER_FLAGS
    "--iree-input-demote-f64-to-f32=false"
  RUNNER_ARGS
    "--expect_failure"
)

iree_check_single_backend_test_suite(
  NAME
    check_failure_llvm-cpu_local-task_generic
  SRCS
    "failure.mlir"
  TARGET_BACKEND
    "llvm-cpu"
  DRIVER
    "local-task"
  COMPILER_FLAGS
    "--iree-input-demote-f64-to-f32=false"
    "--iree-llvmcpu-target-cpu=generic"
  RUNNER_ARGS
    "--expect_failure"
  LABELS
    "nomips"
)

### BAZEL_TO_CMAKE_PRESERVES_ALL_CONTENT_BELOW_THIS_LINE ###
